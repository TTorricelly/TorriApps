# Use this configuration for local development with Vite proxy (no CORS)
# Copy to .env to use proxy mode

# API Configuration - Proxy Mode
# VITE_API_BASE_URL is commented out to use Vite proxy

# Environment
NODE_ENV=development

# PWA Configuration  
VITE_APP_NAME=TorriApps
VITE_APP_SHORT_NAME=TorriApps
VITE_APP_DESCRIPTION=Book appointments with your favorite salon